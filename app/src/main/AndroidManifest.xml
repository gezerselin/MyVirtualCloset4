<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myvirtualcloset3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyVirtualCloset3">
        <activity android:name=".ui.EventListActivity"></activity>
        <activity android:name=".ui.EventActivity" />
        <activity android:name=".ui.clothingDetails.ClothingDetailActivity" />
        <activity android:name=".ui.insideDrawer.InsideDrawerActivity" />
        <activity android:name=".ui.drawer.DrawerActivity" />
        <activity android:name=".ui.addclothing.AddClothingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>